name: Link Checker

on:
  push:
    branches: [development, development_2.6]
  pull_request:
    branches: [development, development_2.6]

jobs:
  check-links-and-create-issue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Link Checker
        id: lychee
        uses: lycheeverse/lychee-action@v1
        with:
          args: --output-stdout
        continue-on-error: true

      - name: Write Output to File
        run: echo "${{ steps.lychee.outputs.stdout }}" > lychee/out.md

      - name: Set Exit Code
        run: echo "::set-output name=lychee_exit_code::${{ steps.lychee.outcome }}"
        id: set-exit-code

      - name: Create Issue
        if: steps.set-exit-code.outputs.lychee_exit_code != 'success'
        uses: peter-evans/create-issue-from-file@v4
        with:
          title: "${{ github.workflow }}: Some links are broken"
          token: ${{ github.token }}
          content-filepath: lychee/out.md
          labels: link_checker